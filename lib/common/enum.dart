enum AnswerQuestion { DEFAULT, CORRECT, WRONG }

enum DestinationCategory { ALAM, BAHARI, BUDAYA }

enum FavoriteOption { Destination, Food, Hotel }

enum ResultState { Loading, NoData, HasData, Error }
